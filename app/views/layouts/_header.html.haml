%header
  %nav
    %ul
      %li= link_to t(:home), root_path, class: current_page?(root_path) ? 'active' : ''
      %li= link_to t(:about), about_path, class: current_page?(about_path) ? 'active' : ''
      - if current_user
        = link_to current_user.name, admin_path()
        = link_to t(:logout), logout_path, method: :delete
